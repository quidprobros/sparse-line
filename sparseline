#!/bin/env bash


# get branch
function gitty()
{
    local status

    if [[ -r .git/HEAD ]]
    then
        branch=$(<.git/HEAD)
        branch="${branch//*\//}"
        status+="${branch}"
    fi

   # export PS1
   # PS1="${out}"
    printf '%s\n' "${status}"
}
export PROMPT_COMMAND=gitty
